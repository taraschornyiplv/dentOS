name: Makefile CI

on:
  push:
    branches: [ '*' ]
  pull_request:
    branches: [ '*' ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: docker-practice/actions-setup-docker@master
    - uses: actions/checkout@v3

    - name: build
      run: make docker
